<!DOCTYPE html>
<html>
<head>
  <title>User Profile</title>
</head>
<body>
  <header><%= link_to "Home", root_path %></header>
  <% provide(:title, @user.name) %>
  <h1>
    <%= @user.name %>
  </h1>
  <div class="wrapper">  
    <div>
      <strong>Screen Name: </strong><%= @user.screen_name %><br>
      <strong>Email: </strong><%= @user.email %>
    </div>
  </div>

</body>
</html>